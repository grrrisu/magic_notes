<h1>Users</h1>

<table>
	<tr>
	<%= for user <- @users do %>
		<td><%= user.id %></td>
		<td><%= render "user.html", user: user %></td>
		<td><%= link "show", to: Routes.user_path(@conn, :show, user.id) %></td>
	</tr>
	<% end %>
</table>